FROM node:lts-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install --only=production        # ✅ pas de devDependencies
RUN npm install pm2 -g                   # PM2 installé globalement, indépendant
COPY . .
EXPOSE 80
CMD ["npm", "run", "prod"]